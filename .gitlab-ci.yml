# Configuration pour déployer un site Docusaurus sur GitLab Pages
image: node:16

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - main # ou "master", selon ta branche principale
